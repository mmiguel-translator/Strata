[build]
  publish = "."
  functions = "netlify/functions"

# Netlify's default Node for functions can be older than 18, where global
# fetch() doesn't exist — pin 18 so the region function works.
[build.environment]
  NODE_VERSION = "18"
